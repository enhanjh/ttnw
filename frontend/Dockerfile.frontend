# Node.js 22 사용
FROM node:22

# 작업 디렉토리 설정
WORKDIR /app

# package.json 및 package-lock.json 복사
COPY package*.json ./

# Node.js 의존성 설치
RUN npm install

# frontend 소스 코드 전체 복사
COPY . .

# 개발 서버 시작 (개발 환경용)
CMD ["npm", "start"]

# 프로덕션 빌드 시에는 다음과 같이 변경 (선택 사항)
# RUN npm run build
# CMD ["serve", "-s", "build"] # `serve` 패키지 필요
